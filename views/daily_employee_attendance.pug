head
  title Cressoft AMS Daily Attendance
  link(rel='stylesheet', href='/style.css')
  script.
    function validateDate() {
      const dateInput = document.getElementById('day');
      const dateValue = dateInput.value;
      const dateRegex = /^\d{4}-\d{2}-\d{2}$/; // YYYY-MM-DD format

      if (!dateRegex.test(dateValue)) {
        alert('Please enter a date in YYYY-MM-DD format.');
        return false; // Prevent form submission
      }

      return true; // Allow form submission
    }
body
  include header1.pug
  h2.center.scaps Dear #{userName} fill the details
  br
  form(method='post' action='/daily_employee_attendance' class='' onsubmit='return validateDate();')
    label(for='name') Name:
    input(type='text', name='name',required=true,value=userName, readonly)
    label(for="date") Date:
    input(type="date" name="date" id="day" min="2022-01-01" required placeholder="YYYY-MM-DD")
    p.gorg
      button(type='submit' class="button") Submit
